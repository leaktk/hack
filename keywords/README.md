# Keywords

Here are some keywords files that I'm not quite ready to integrate into the patterns.
Might be useful for adhoc checks. Also the format is done in a way where they can easily be
used with tools like grep/ripgrep etc with the `-f` flag.
